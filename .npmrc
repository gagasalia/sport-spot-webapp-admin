# @angular/google-maps 21 declares an Angular 21/22 peer range while this app is
# pinned to Angular 20; the package works fine against 20 at runtime. Allow the
# install to proceed so `npm ci` (local and CI) does not fail on the peer mismatch.
legacy-peer-deps=true
